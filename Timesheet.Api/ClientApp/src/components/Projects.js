import * as React from 'react';

class Projects extends React.PureComponent {
    render() {
        return (
            <React.Fragment>
                <h1>Projects</h1>
            </React.Fragment>
        );
    }
};

export default Projects;
